{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window capability — grants access to core IPC and all app commands.",
  "windows": ["main*"],
  "permissions": [
    "core:default"
  ]
}
